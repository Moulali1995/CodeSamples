var array = [1, 2, 3, 4, 5]
// slice to return a portion of array
var arraySlice = array.slice(0, 2)
print(arraySlice)
print(array)
// splice to remove a portion of array
var arraySplice = array.splice(0, 2)
print(arraySplice)
print(array)
function print (printArray) {
  console.log(printArray)
}
